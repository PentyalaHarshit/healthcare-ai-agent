services:
  nginx:
    ports:
      - "80:80"
    volumes:
      - ./deploy/nginx/templates/http-only.conf.template:/etc/nginx/templates/default.conf.template:ro
      - ./deploy/certbot/www:/var/www/certbot:ro
      - ./deploy/certbot/conf:/etc/letsencrypt:ro
